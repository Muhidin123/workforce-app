<%= form_with(model: break) do |form| %>
  <% if break.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(break.errors.count, "error") %> prohibited this break from being saved:</h2>

      <ul>
        <% break.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
